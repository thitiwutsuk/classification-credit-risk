# หลักการทำงานของ Data Science ในโปรเจกต์นี้

เอกสารนี้อธิบายว่าใน [`analysis.py`](analysis.py) ใช้ **เทคนิค** และ **โมเดล** อะไรบ้าง ในแต่ละขั้นตอน และทำไมถึงเลือกใช้แบบนั้น เพื่อให้คนที่ไม่ได้เขียนโค้ดเองก็เข้าใจหลักคิดเบื้องหลังได้

## ภาพรวมกระบวนการ (Data Science Workflow)

```
1. รวบรวม/รวมข้อมูล (Aggregation & Merge)
        ↓
2. ทำความสะอาดข้อมูล (Data Cleaning)
        ↓
3. สำรวจข้อมูล (Exploratory Data Analysis)
        ↓
4. สร้าง feature ใหม่ (Feature Engineering)
        ↓
5. เตรียมข้อมูลสำหรับโมเดล (Preprocessing)
        ↓
6. เทรนโมเดล (Modeling)
        ↓
7. วัดผลโมเดล (Evaluation)
        ↓
8. แปลผลเป็นภาษาคน (Interpretation)
```

แต่ละขั้นมีเทคนิคเฉพาะของมัน อธิบายทีละขั้นด้านล่าง

---

## 1. การรวมข้อมูล (Aggregation)

**ปัญหา**: `payment_data.csv` มีหลายแถวต่อลูกค้า 1 คน (คนละผลิตภัณฑ์/รอบรายงาน) แต่เราต้องการข้อมูลแค่ **1 แถวต่อคน** เพื่อเอาไปจับคู่กับ label

**เทคนิคที่ใช้**: `groupby("id").agg(...)` ของ pandas โดยเลือกวิธีสรุปให้เหมาะกับความหมายของแต่ละคอลัมน์:

| ประเภทข้อมูล | วิธีสรุป | เหตุผล |
|---|---|---|
| จำนวนครั้งค้างจ่าย, จำนวนครั้งจ่ายปกติ (`OVD_t1/t2/t3`, `pay_normal`) | **sum** | อยากรู้ "รวมทั้งหมด" ที่เกิดขึ้นกับลูกค้าคนนี้ |
| วงเงิน/ยอดคงเหลือ (`prod_limit`, `new_balance`, `highest_balance`) | **mean และ max** | mean บอกพฤติกรรมทั่วไป, max บอก "จุดสูงสุด" ที่เคยเกิดขึ้น (สัญญาณความเสี่ยงมักซ่อนอยู่ในค่าสูงสุด ไม่ใช่ค่าเฉลี่ย) |
| รหัสผลิตภัณฑ์ (`prod_code`) | **nunique** (นับจำนวนไม่ซ้ำ) | อยากรู้ว่าลูกค้าถือผลิตภัณฑ์กี่ประเภท ไม่ใช่ผลรวมของรหัส |

---

## 2. การทำความสะอาดข้อมูล (Data Cleaning)

**ปัญหา**: หลัง merge แล้วมีค่าหายไป (missing values) ในบางคอลัมน์

**เทคนิคที่ใช้**: เลือกวิธีเติมค่า (imputation) ตามความหมายของข้อมูล ไม่ใช้วิธีเดียวกันทั้งหมด:

- **เติมด้วย 0** สำหรับคอลัมน์ที่เป็น "จำนวนครั้ง/นับ" (เช่น `ovd_t1_sum`) — เพราะไม่มีข้อมูล = ไม่เคยเกิดเหตุการณ์นั้นกับลูกค้าคนนี้เลย
- **เติมด้วยค่ากลาง (median)** สำหรับคอลัมน์ที่เป็น "จำนวนเงิน" (เช่น `prod_limit_mean`) — ใช้ median แทน mean เพราะ median **ทนทานต่อ outlier** (ค่าผิดปกติสุดโต่ง) มากกว่า ถ้าใช้ mean แล้วมีลูกค้ารายใหญ่ 1-2 คนที่มียอดสูงมาก จะทำให้ค่าเฉลี่ยเบี้ยวไปทั้งชุด

---

## 3. การสำรวจข้อมูล (EDA)

**เทคนิคที่ใช้**:
- **Group-by mean comparison**: เทียบค่าเฉลี่ยของแต่ละ feature ระหว่างกลุ่ม label=0 (Worthy) กับ label=1 (Risky) — เป็นวิธีที่ตรงไปตรงมาที่สุดในการหา "สัญญาณ" เบื้องต้น
- **Percentage difference (% diff)**: คำนวณส่วนต่างเป็น % แทนตัวเลขดิบ เพราะ feature แต่ละตัวมีหน่วย/สเกลต่างกันมาก (บางตัวหลักแสน บางตัวหลักหน่วย) การใช้ % ทำให้เทียบ "ขนาดผลกระทบ" ข้าม feature ได้อย่างยุติธรรม
- **Correlation coefficient**: วัดความสัมพันธ์เชิงเส้นระหว่างแต่ละ feature กับ `label` (ค่าอยู่ระหว่าง -1 ถึง +1) ยิ่งใกล้ ±1 ยิ่งสัมพันธ์กันแรง
- **Crosstab (สำหรับ categorical feature)**: ดูว่าสัดส่วนของแต่ละหมวดหมู่ (เช่น fea_6 = 8, 11, 15) ต่างกันระหว่าง 2 กลุ่มไหม

---

## 4. การสร้าง Feature ใหม่ (Feature Engineering)

สร้างตัวแปรใหม่ที่ไม่ได้มีอยู่ในข้อมูลดิบ แต่ช่วยให้โมเดล "เห็น" พฤติกรรมได้ชัดขึ้น:

```
overdue_ratio = (จำนวนครั้งค้างจ่ายทั้งหมด) / (จำนวนครั้งค้างจ่าย + จำนวนครั้งจ่ายปกติ)
```

**เหตุผล**: ตัวเลข "จำนวนครั้งค้างจ่าย" ดิบๆ เพียงอย่างเดียวทำให้เข้าใจผิดได้ เช่น ลูกค้า A ค้างจ่าย 5 ครั้งจาก 100 ครั้ง (ค้างจ่ายน้อย) กับลูกค้า B ค้างจ่าย 5 ครั้งจาก 6 ครั้ง (ค้างจ่ายบ่อยมาก) มีจำนวนค้างจ่ายเท่ากันแต่พฤติกรรมต่างกันสุดขั้ว — `overdue_ratio` แก้ปัญหานี้โดยดู "สัดส่วน" แทน "จำนวนดิบ"

---

## 5. การเตรียมข้อมูลสำหรับโมเดล (Preprocessing)

### 5.1 One-Hot Encoding
Feature ประเภท categorical (`fea_1, fea_3, fea_5, fea_6, fea_7, fea_9`) เป็นตัวเลขรหัส (เช่น 1, 2, 3) แต่ **ไม่มีลำดับความหมาย** (รหัส 3 ไม่ได้ "มากกว่า" รหัส 1) ถ้าปล่อยให้โมเดลอ่านเป็นตัวเลขตรงๆ โมเดลจะเข้าใจผิดว่ามันมีลำดับ จึงต้องแปลงเป็น **one-hot encoding** คือสร้างคอลัมน์ใหม่แยกสำหรับแต่ละหมวดหมู่ (เป็น 0/1) แทน

### 5.2 Train/Test Split แบบ Stratified
แบ่งข้อมูล 75% สำหรับเทรน (train) และ 25% สำหรับทดสอบ (test) โดยใช้ `stratify=y` เพื่อรักษาสัดส่วน 80/20 (Worthy/Risky) ให้เท่ากันทั้งใน train และ test set — ถ้าไม่ทำแบบนี้ อาจสุ่มได้ test set ที่มีกลุ่มเสี่ยงน้อยผิดปกติ ทำให้วัดผลไม่แม่น

### 5.3 Feature Scaling (StandardScaler)
ใช้เฉพาะกับ Logistic Regression: แปลงทุก feature ให้มี mean = 0, standard deviation = 1 เหตุผลคือ Logistic Regression เปรียบเทียบ "น้ำหนัก" (coefficient) ของแต่ละ feature กัน ถ้าไม่ scale ก่อน feature ที่มีสเกลใหญ่ (เช่น ยอดเงินหลักแสน) จะดู "สำคัญ" กว่าที่ควรจะเป็นเมื่อเทียบกับ feature สเกลเล็ก (เช่น จำนวนครั้ง 0-10) ทั้งที่ในความเป็นจริงอาจไม่ได้สำคัญกว่ากันเลย

*(หมายเหตุ: Random Forest ไม่ต้อง scale เพราะ decision tree ตัดสินใจจาก "ค่ามากกว่า/น้อยกว่า threshold" ไม่ใช่ระยะห่างเชิงตัวเลข จึงไม่ถูกสเกลที่ต่างกันหลอก)*

### 5.4 Class Weight Balancing
ข้อมูลไม่สมดุล (80% Worthy, 20% Risky) ถ้าไม่แก้ไข โมเดลจะ "ขี้เกียจ" เรียนรู้กลุ่มเสี่ยง เพราะแค่ทายว่า "ไม่เสี่ยง" ตลอดก็ได้ accuracy 80% แล้ว ใช้ `class_weight="balanced"` เพื่อให้โมเดลถ่วงน้ำหนักความผิดพลาดของกลุ่มเสี่ยง (ส่วนน้อย) ให้มีค่ามากกว่า บังคับให้โมเดลสนใจกลุ่มนี้มากขึ้น

---

## 6. โมเดลที่ใช้ (Modeling)

ใช้ 2 โมเดลที่มีจุดแข็งต่างกัน เพื่อ "เช็คไขว้" (cross-check) ผลลัพธ์ระหว่างกัน

### 6.1 Logistic Regression

**หลักการ**: หาความสัมพันธ์เชิงเส้นระหว่าง feature กับความน่าจะเป็นที่จะเป็น "เสี่ยง" โดย:

1. รวม feature ทุกตัวถ่วงน้ำหนักบวกกันเป็นค่า `z` (คล้ายสมการเส้นตรง `z = w1*x1 + w2*x2 + ... + bias`)
2. บีบค่า `z` (ซึ่งมีค่าได้ตั้งแต่ -∞ ถึง +∞) ให้อยู่ในช่วง 0-1 ด้วย **sigmoid function**: `probability = 1 / (1 + e^-z)`
3. ถ้า probability ≥ 0.5 → ทำนายว่า "เสี่ยง", ถ้าน้อยกว่า → ทำนายว่า "ไม่เสี่ยง"
4. ตอนเทรน โมเดลจะปรับน้ำหนัก (w) ไปเรื่อยๆ ด้วยวิธี **gradient descent** เพื่อให้ทำนายใกล้เคียงคำตอบจริงมากที่สุด

**จุดแข็ง**: ตีความง่าย — ดูจากเครื่องหมาย (+/-) และขนาดของ coefficient แต่ละตัวได้ทันทีว่า feature นั้นเพิ่มหรือลดความเสี่ยง และมากน้อยแค่ไหน

### 6.2 Random Forest

**หลักการ**: เป็น **ensemble model** (โมเดลรวมหมู่) ที่สร้าง decision tree (ต้นไม้ตัดสินใจ) หลายร้อยต้น (ในโค้ดใช้ 300 ต้น — `n_estimators=300`) โดยแต่ละต้น:
- เห็นข้อมูลและ feature คนละชุดแบบสุ่ม (bootstrap sampling + random feature selection) เรียกว่าเทคนิค **Bagging**
- ต้นไม้แต่ละต้น "โหวต" ว่าลูกค้าคนนี้เสี่ยงหรือไม่ แล้วเอาเสียงส่วนใหญ่มาเป็นคำตอบสุดท้าย

จำกัดความลึกของต้นไม้ไว้ที่ `max_depth=6` เพื่อไม่ให้แต่ละต้นซับซ้อนเกินไปจนจำข้อมูล train ได้หมด (overfitting) — เหมือนบังคับให้แต่ละต้น "ตัดสินใจแบบหยาบๆ" แล้วอาศัยจำนวนต้นเยอะๆ มาช่วยกันตัดสินให้แม่นขึ้นแทน

**จุดแข็ง**: จับความสัมพันธ์ที่ไม่เป็นเส้นตรง (non-linear) และปฏิสัมพันธ์ระหว่าง feature ได้ดีกว่า Logistic Regression และให้ค่า **feature importance** (ความสำคัญของแต่ละ feature วัดจากว่ามันถูกใช้ "แบ่งข้อมูล" ในต้นไม้บ่อยและมีประสิทธิภาพแค่ไหน) มาโดยอัตโนมัติ

### เปรียบเทียบ 2 โมเดล

| | Logistic Regression | Random Forest |
|---|---|---|
| รูปแบบความสัมพันธ์ | เชิงเส้น (linear) | ไม่เชิงเส้น (non-linear) |
| ตีความง่ายแค่ไหน | ง่ายมาก (ดู coefficient) | ปานกลาง (ดู feature importance) |
| บอกทิศทางได้ไหม (เพิ่ม/ลดความเสี่ยง) | ได้ (เครื่องหมาย +/-) | ไม่ได้โดยตรง (บอกแค่ "สำคัญแค่ไหน") |
| ต้อง scale ข้อมูลก่อนไหม | ต้อง | ไม่ต้อง |

---

## 7. การวัดผลโมเดล (Evaluation)

- **Accuracy**: สัดส่วนที่ทำนายถูกทั้งหมด — *ระวัง*: ข้อมูลไม่สมดุล (80/20) ทำให้ accuracy สูงได้ง่ายๆ แค่ทายกลุ่มใหญ่ตลอด จึงไม่ใช่ตัวชี้วัดที่น่าเชื่อถือที่สุดสำหรับข้อมูลแบบนี้
- **ROC-AUC**: วัดว่าโมเดล "แยกแยะ" ระหว่าง 2 กลุ่มได้ดีแค่ไหน โดยไม่สนใจ threshold ตายตัว (0.5) — ค่า 1.0 = แยกได้สมบูรณ์แบบ, 0.5 = เดาสุ่ม เหมาะกับข้อมูลไม่สมดุลมากกว่า accuracy
- **Confusion Matrix**: ตารางเทียบคำตอบจริงกับคำตอบที่ทำนาย แยกเป็น 4 ช่อง (ทายถูกว่าไม่เสี่ยง, ทายผิดว่าเสี่ยงทั้งที่ไม่เสี่ยง, ทายผิดว่าไม่เสี่ยงทั้งที่เสี่ยงจริง, ทายถูกว่าเสี่ยง) — ช่วยเห็นว่าโมเดลพลาดแบบไหนบ่อยกว่ากัน
- **Precision / Recall / F1-score**: โฟกัสเฉพาะกลุ่มเสี่ยง — Precision บอกว่า "ที่ทายว่าเสี่ยง ถูกจริงกี่ %", Recall บอกว่า "ในกลุ่มเสี่ยงจริงทั้งหมด จับได้กี่ %" ในบริบทธนาคาร **Recall ของกลุ่มเสี่ยงมักสำคัญกว่า** เพราะการปล่อยลูกค้าเสี่ยงหลุดไป (false negative) มักสร้างความเสียหายมากกว่าการระแวงลูกค้าดีเกินจำเป็น (false positive)

---

## 8. การจัดอันดับปัจจัย (Feature Ranking) และแปลผล

รวมสัญญาณจาก 3 แหล่งเพื่อความน่าเชื่อถือ (triangulation):
1. **Logistic Regression coefficient** — บอกทิศทาง + ความแรงเชิงเส้น
2. **Random Forest feature importance** — บอกความสำคัญโดยรวม (ไม่สนทิศทาง)
3. **% difference จาก EDA** — ค่าเฉลี่ยจริงต่างกันแค่ไหนระหว่าง 2 กลุ่ม

ถ้าทั้ง 3 แหล่งชี้ไปทางเดียวกัน (เช่น ทุกตัวบอกว่า `overdue_ratio` สูง → เสี่ยงขึ้น) ถือว่าเป็นสัญญาณที่ **น่าเชื่อถือมาก** เพราะไม่ได้พึ่งพาวิธีวัดแบบเดียว

---

## ข้อจำกัดของแนวทางนี้

- **Correlation ≠ Causation**: ผลลัพธ์บอกว่า feature ไหน "สัมพันธ์" กับความเสี่ยง ไม่ได้พิสูจน์ว่าเป็น "สาเหตุ" โดยตรง
- **ขนาดข้อมูลเล็ก** (1,125 คน) ทำให้โมเดลมีความไม่แน่นอนสูงกว่าข้อมูลขนาดใหญ่
- **ข้อมูลไม่สมดุล** (20% เสี่ยง) ทำให้โมเดลเรียนรู้ pattern ของกลุ่มเสี่ยงได้ยากกว่ากลุ่มไม่เสี่ยงโดยธรรมชาติ แม้จะช่วยด้วย `class_weight="balanced"` แล้วก็ตาม
