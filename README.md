# Credit Risk Analysis

![Python](https://img.shields.io/badge/Python-3.13-3776AB?style=for-the-badge&logo=python&logoColor=white&labelColor=1e2130)
![pandas](https://img.shields.io/badge/pandas-2.3-150458?style=for-the-badge&logo=pandas&logoColor=white&labelColor=1e2130)
![NumPy](https://img.shields.io/badge/NumPy-2.3-013243?style=for-the-badge&logo=numpy&logoColor=white&labelColor=1e2130)
![scikit-learn](https://img.shields.io/badge/scikit--learn-1.7-F7931E?style=for-the-badge&logo=scikitlearn&logoColor=white&labelColor=1e2130)
![Matplotlib](https://img.shields.io/badge/Matplotlib-3.10-11557C?style=for-the-badge&logo=matplotlib&logoColor=white&labelColor=1e2130)
![Seaborn](https://img.shields.io/badge/Seaborn-0.13-3776AB?style=for-the-badge&labelColor=1e2130)
![Jupyter](https://img.shields.io/badge/Jupyter-Notebook-F37626?style=for-the-badge&logo=jupyter&logoColor=white&labelColor=1e2130)

A data science project analyzing bank customer data to identify what separates **Credit Risky** customers from **Credit Worthy** ones, using exploratory data analysis and supervised classification.

## Table of Contents

- [Business Questions](#business-questions)
- [Dataset](#dataset)
- [Project Structure](#project-structure)
- [Development Progress](#development-progress)
- [Getting Started](#getting-started)
- [Results](#results)
- [Limitations](#limitations)

## Business Questions

From a banking risk-management perspective, this project answers:

- **Q1** — What factors contribute to a customer being Credit Risky?
- **Q2** — What behavior distinguishes a Credit Worthy customer?

## Dataset

Two source files live in [`data/`](data/) (full column-level documentation in [`data/dataset_description.md`](data/dataset_description.md)):

| File | Rows | Description |
|---|---|---|
| `data/customer_data.csv` | 1,125 customers | Demographic and encoded categorical features, plus the target `label` (`1` = high risk, `0` = low risk / worthy) |
| `data/payment_data.csv` | 8,250 records | Card payment history — overdue counts/days, normal payment counts, product limits, and balances. A customer can hold multiple products, so this table has multiple rows per customer |

## Project Structure

```
.
├── analysis.py                  # end-to-end analysis script
├── credit_risk_analysis.ipynb   # notebook walkthrough with step-by-step explanations (Thai)
├── requirements.txt             # Python dependencies
├── METHODOLOGY.md               # in-depth walkthrough of techniques & models (Thai)
├── data/
│   ├── customer_data.csv
│   ├── payment_data.csv
│   └── dataset_description.md   # column-by-column reference
└── output/                      # charts and summary tables generated by the script
```

> New to the project or the underlying data science? [`credit_risk_analysis.ipynb`](credit_risk_analysis.ipynb) walks through every step below interactively, in Thai, with each explanation right next to the code and chart it produces.

## Development Progress

[`analysis.py`](analysis.py) follows a standard applied data science workflow. Progress is tracked below against each stage of that workflow, summarizing what was completed and why.

### 1. Load & Aggregate
- [x] Loaded `customer_data.csv` (1,125 rows, one per customer) and `payment_data.csv` (8,250 rows, multiple per customer)
- [x] Aggregated payment history to one row per customer `id`:
  - Overdue counts and normal-payment counts summed (total activity per customer)
  - Balances and limits summarized with mean **and** max (typical level vs. worst/highest exposure)
  - Product codes counted with `nunique` (number of distinct products held)
- [x] Merged the aggregated payment data into `customer_data` on `id`

### 2. Clean the Data
- [x] Audited missing values post-merge and confirmed their source (customers with no payment record, or unreported balances)
- [x] Filled count-like fields (e.g. overdue counts) with `0` — no record means the event never occurred
- [x] Filled monetary fields (limits, balances) with the **median**, chosen for robustness against outlier balances
- [x] Verified zero remaining missing values before proceeding

### 3. Exploratory Data Analysis (EDA)
- [x] Compared every feature's mean between the Risky (`label=1`) and Worthy (`label=0`) groups
- [x] Normalized gaps as a **percentage difference** so features on very different scales are comparable
- [x] Cross-tabulated categorical features against the label to check for demographic effects
- [x] Computed correlation of each numeric feature with the target
- [x] Exported 3 charts: class balance, top-feature boxplots by group, correlation bar chart

### 4. Feature Engineering
- [x] Derived `overdue_ratio = overdue count / (overdue count + normal payment count)`
- [x] Rationale: raw overdue counts are misleading in isolation (5 of 100 vs. 5 of 6 payments overdue are very different customers) — the ratio captures behavior, not just raw tally

### 5. Preprocessing
- [x] One-hot encoded categorical codes (`fea_1, fea_3, fea_5, fea_6, fea_7, fea_9`), which carry no ordinal meaning
- [x] Split the data 75/25 into train/test with **stratification** to preserve the 80/20 class ratio
- [x] Standardized features (`StandardScaler`) ahead of Logistic Regression for comparable coefficients
- [x] Applied `class_weight="balanced"` to both models to counteract class imbalance

### 6. Modeling
- [x] Trained two models so their conclusions can be cross-checked against each other:

  | | Logistic Regression | Random Forest |
  |---|---|---|
  | Relationship captured | Linear | Non-linear, with interactions |
  | Interpretability | High — coefficient sign & magnitude | Moderate — feature importance |
  | Gives direction (risk ↑/↓)? | Yes | No (importance only) |
  | Needs feature scaling? | Yes | No |

### 7. Evaluation
- [x] Selected **ROC-AUC** as the primary metric, since accuracy alone is misleading on imbalanced classes (always predicting "Worthy" already scores 80%)
- [x] Generated a confusion matrix and per-class precision/recall/F1 for both models
- [x] Prioritized recall on the Risky class, since missing a genuinely risky customer (false negative) is typically costlier than over-flagging a good one (false positive)

### 8. Interpretation
- [x] Triangulated feature rankings from three independent signals: logistic regression coefficients, random forest importances, and raw EDA percentage gaps
- [x] Reported a factor as a genuine driver of risk only where all three signals agreed
- [x] Translated the ranked factors into the plain-language Q1/Q2 answers below

> For a deeper, more technical walkthrough of every technique (with the underlying math for the sigmoid function, bagging, gradient descent, etc.), see [`METHODOLOGY.md`](METHODOLOGY.md).

## Getting Started

Requires Python 3 (a virtual environment is recommended).

```bash
cd "classification credit risk"
pip install -r requirements.txt
python3 analysis.py
```

This prints all statistics, model metrics, and the Q1/Q2 narrative to the console, and saves 4 charts plus a feature-importance summary table to `output/`.

## Results

### Q1 — What makes a customer Credit Risky?

Risk is driven primarily by **payment behavior**, not demographics:

- **Higher outstanding balances** — risky customers' `highest_balance` runs more than 2x higher on average than worthy customers'
- **More frequent overdue payments** — both `overdue_ratio` and total overdue days are markedly higher
- **Fewer on-time payments** — `pay_normal_sum` is roughly 30% lower than the worthy group
- Encoded demographic features (`fea_1, fea_3, fea_5, fea_6, fea_7, fea_9`) show comparatively **minor** differences between groups

**In short**: high debt + frequent overdue payments + inconsistent on-time payments define the core risky profile.

### Q2 — How does a Credit Worthy customer behave?

The mirror image of the risky profile:

- Keeps outstanding balances **low relative to available credit**
- Is **rarely overdue** — average overdue ratio around 3%
- Pays **consistently and on time**, with a clearly higher count of normal payments

**In short**: consistent on-time payment behavior and controlled debt, not demographic background, are what mark a customer as credit worthy.

## Limitations

- Model discriminative power (ROC-AUC ≈ 0.68) indicates real but moderate predictive signal — expected given the dataset's small size (1,125 customers) and class imbalance (only 20% risky)
- These are **correlational**, not **causal**, findings — e.g. a high balance can be both a symptom and a cause of risk. Treat results as a decision-support signal, not a standalone verdict
